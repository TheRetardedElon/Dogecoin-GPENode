Place headless Windows binaries here before install-service.ps1:

  dogecoind.exe
  dogecoin-cli.exe

Build from dogedev (Core Pro DNA) without shipping Qt for this product line.
Do not commit .exe files to public git (large / release artifacts).
